CREATE FUNCTION DBO.fcAjustaNonoDigito (@FONE VARCHAR(14))
RETURNS VARCHAR(14)
AS
BEGIN
	IF(LEN(@FONE) = 10 AND CAST(SUBSTRING(@FONE,3,1) AS TINYINT)>=6 AND SUBSTRING(@FONE,1,4)<>'0800'))
		BEGIN
			SET @FONE = CONCAT(SUBSTRING(@FONE,1,2),'9',SUBSTRING(@FONE,3,8))
		END
    RETURN @FONE;
END;

