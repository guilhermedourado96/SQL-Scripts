Select 
OBJ.NAME AS TABELA, 
C.NAME AS COLUNA 
FROM SYS.SYSOBJECTS OBJ
	INNER JOIN SYS.COLUMNS C
	ON OBJ.ID = C.OBJECT_ID
	INNER JOIN SYS.TABLES T
	ON OBJ.ID = T.OBJECT_ID

WHERE 	OBJ.TYPE = 'U'
	AND (C.NAME LIKE '%%' OR T.NAME LIKE '%%')

